from servo import Servo
from machine import PWM

class Servo_Sys:
    def __init__ (self, speed, forward, back, left, right, idle, debug=False):
        self.__speed = speed
        self.__forward = forward
        self.__back = back
        self.__left = left
        self.__right = right
        self.__idle = idle
        self.__debug = debug

    def Idle(self):
        if self
        

    def Move_forward(self):

    def R_Turn(self):

    def L_Turn(self):

    def Backward(self):